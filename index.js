// Файл с ошибками для проверки ESLint и Prettier

const unusedVar = 'эта переменная не используется';

const func = () => {
const sum=(a,       b)=> {  // неправильное форматирование
  return a+b;
};
let x=10;  // нет пробелов
let y=20;  // нет пробелов

console.log(  sum( x, y )  ); // лишние пробелы
debugger;
x=   20;  // нет пробелов
y=   30;  // нет пробелов

console.log(  sum( x, y )  ); // лишние пробелы
};

func();

const a = "строка в двойных кавычках"; // должны быть одинарные
const b = `шаблонная строка с ${a}`;